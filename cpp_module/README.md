# C++ Module
Sample integration for calling the Python NLP core from C++.

## Usage
- Implement `call_python_nlp()` to connect with the Python module (e.g., via REST/gRPC or Python/C API).
- Build with a C++17+ compiler.
